import 'package:flutter/material.dart';

class MassagesPage extends StatelessWidget {
  const MassagesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('MassgesPage'),
    );
  }
}
